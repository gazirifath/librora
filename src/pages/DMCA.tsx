import DynamicPage from "@/components/DynamicPage";

const DMCA = () => (
  <DynamicPage
    slug="dmca"
    fallbackTitle="DMCA Policy"
    fallbackContent="<p>Loading DMCA policy content...</p>"
  />
);

export default DMCA;
